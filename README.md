# Installation:

    git clone git://github.com/marcogaiarsa/dotvim.git ~/vim

# Create symlink:

    ln -s ~/dotfiles/vimrc ~/.vimrc
    
# Get submodules:

    git submodule init
    git submodule update
